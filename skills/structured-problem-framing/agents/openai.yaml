interface:
  display_name: "Structured Problem Framing"
  short_description: "Choose the right structure for an accepted problem."
  default_prompt: "Use $structured-problem-framing to define this accepted working problem, compare at least two representations, choose the best structure, state hypotheses and evidence needs, and stop before full solution design."

policy:
  allow_implicit_invocation: true
